require 'puppetlabs_spec_helper/rake_tasks'
require 'puppet-syntax/tasks/puppet-syntax'
require 'yaml'
<% CONFIG = YAML.load_file(".sync.yml")
CONFIG['Rakefile']['extra'].each do |item| %>
<%=item-%>
<% end %>
